# Database Configuration
DB_NAME=ecommerce_db
DB_USER=root
DB_PASSWORD=SecurePassword123!
DB_HOST=db
DB_PORT=3306

# Django Configuration
DEBUG=False
SECRET_KEY=change-this-to-a-random-50-character-secret-key-for-production
ALLOWED_HOSTS=localhost,127.0.0.1,your-domain.com

# CORS Configuration (Frontend URL)
CORS_ALLOWED_ORIGINS=http://localhost

# Razorpay Configuration (Payment Gateway)
RAZORPAY_KEY_ID=rzp_test_JXRn3gzDuDb3AM
RAZORPAY_KEY_SECRET=5VbARbWYrPrn29MGeB4Jff4r

# IMPORTANT NOTES:
# 1. Copy this file to .env before running: cp .env.example .env
# 2. Change all passwords and secret keys to secure random values
# 3. Update ALLOWED_HOSTS with your actual domain name when deploying
# 4. Update CORS_ALLOWED_ORIGINS with your actual frontend URL
# 5. Update Razorpay keys with your production keys if using real payments
# 6. NEVER commit .env file to version control
